
/*

describe('is_same_client', () => {
  const all_clients = [    {      name: 'Alice',      accounts: [{ account_number: 1234 }, { account_number: 5678 }]
    },
    {
      name: 'Bob',
      accounts: [{ account_number: 9999 }]
    }
  ];

  it('should return true if target account belongs to a client in all_clients', () => {
    const target_account = '5678';
    const result = is_same_client(target_account, all_clients);
    expect(result).toBe(true);
  });

  it('should return false if target account does not belong to any client in all_clients', () => {
    const target_account = '1111';
    const result = is_same_client(target_account, all_clients);
    expect(result).toBe(false);
  });

  it('should handle target_account as a string or number', () => {
    let target_account = '1234';
    let result = is_same_client(target_account, all_clients);
    expect(result).toBe(true);

    target_account = 5678;
    result = is_same_client(target_account, all_clients);
    expect(result).toBe(true);
  });

});

 */

