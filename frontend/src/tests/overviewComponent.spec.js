
//import { mount } from '@vue/test-utils'
//import OverviewComponent from '@/components/OverviewComponent.vue'

/*
describe('OverviewComponent', () => {
    test('zobrazuje správné informace o klientovi', () => {
        const wrapper = mount(OverviewComponent)

        expect(wrapper.html()).toContain('<h3>Jméno: Test</h3>')
        expect(wrapper.html()).toContain('<h3>Příjmení: Testovací</h3>')
        expect(wrapper.html()).toContain('<h3>Účet: 1234567890</h3>')
        expect(wrapper.html()).toContain('<h3>Měna: CZK</h3>')
        expect(wrapper.html()).toContain('<h3>Stav účtu: 0 CZK</h3>')
    })
})


describe('OverviewComponent_2', () => {
    test('vytvoření nového účtu', async () => {
        const wrapper = mount(OverviewComponent)

        await wrapper.find('button:contains("Vytvořit nový účet")').trigger('click')
        expect(wrapper.html()).toContain('Vytvoření nového účtu')
        await wrapper.find('select').setValue('USD')
        await wrapper.find('button:contains("Vytvořit nový účet")').trigger('click')
        expect(wrapper.html()).toContain('<h3>Číslo účtu: 1234567891 Měna: USD')
    })
})


 */
